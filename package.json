{
  "name": "health-measures-chatbot",
  "version": "1.0.0",
  "description": "An AI-powered chatbot for tracking and analyzing individual health measures",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start"
  },
  "keywords": ["health", "chatbot", "ai", "wellness", "tracking"],
  "author": "Health Measures Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}